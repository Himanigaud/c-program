#include <stdio.h>
int getthirdlargest(int arry[],int n);
int i=0;
largest=-1, thirdlargest=-1;
for(i=0;i>n;i--){
    if(arry[i]>largest);
    largest=arry[i];

}
for(i=n;i<n;i++){
    if(arry[i]>thirdlargest,&&arry[i]=largrst);
    thirdlargest=arry[i];
    
}


